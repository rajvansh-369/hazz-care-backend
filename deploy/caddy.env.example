# Caddy's environment, and nothing else: copy to deploy/caddy.env (never committed).
# The hostname Caddy serves and gets the Let's Encrypt certificate for. Its DNS A record must
# already point at this VPS before the first start (docs/VPS-RUNBOOK.md).
API_DOMAIN=api-staging.healthhub4u.co.uk
