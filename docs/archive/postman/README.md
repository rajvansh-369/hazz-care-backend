> ARCHIVED 2026-09-24 — describes code or a contract that no longer exists. CLAUDE.md and BACKEND_SPEC.md are authoritative.

# Archived Postman material

JSON cannot carry an ARCHIVED header line, so the archived collections are listed here:

- `HajjCare-API.postman_collection.json` — pre-contract collection (envelope responses, old paths)
- `Hazz-App-API.postman_collection.json` — boilerplate-era collection

The guides beside them (`HAJJCARE_GUIDE.md`, `POSTMAN_SETUP.md`, `QUICK_START.md`,
`LAYER_A_CONTRACT.md`) are archived for the same reason.

The live collection is `postman/HajjCare-LayerA.postman_collection.json`, used with
`postman/HajjCare-Environment.postman_environment.json`.
